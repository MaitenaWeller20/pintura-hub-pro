# SDD ledger — plan: docs/superpowers/plans/2026-08-28-nota-credito-fiscal-por-periodo.md

Workspace: `/private/tmp/quimex-cierre-fiscal.vQKiY2`
Branch start for implementation: `0ff7638`
Spec: `docs/superpowers/specs/2026-08-28-nota-credito-fiscal-por-periodo-design.md`
Baseline: `npm test` — 72 files passed, 1 skipped; 1452 tests passed, 19 skipped.
Baseline observation: local Node 20.20.2 emits EBADENGINE warnings for TanStack packages requiring Node >=22.12.0; tests pass. Task 1's typecheck/build remain the acceptance gate. No dependency or lockfile change retained.

## Pre-flight self-consistency scan

| Task | Produces/tests | Consumes/implementation | Finding |
|---|---|---|---|
| 1 | Separate enum migration and `t` assertion | No other statement uses the enum in the same migration | Consistent; isolates PostgreSQL enum visibility. |
| 2 | Schema/RLS/guard contract | Flag, permission, period columns, payload hash, intent table and guards | Consistent; all asserted fields are created here. |
| 3 | Pure RED/GREEN tests | Strict union, cents, dates, A/B/C and internal-note classifier | Consistent; browser input does not supply fiscal issue date. |
| 4 | SQL integration RED/GREEN | Pending creation, canonical hash and no effects | Consistent; migration variable is declared before commit step. |
| 5 | TS and SQL parity tests | Snapshot v3 plus version dispatcher and checks | Consistent; settlement intentionally remains outside the approved fiscal snapshot. |
| 6 | Adapter/runtime/reconciliation tests | `PeriodoAsoc` request and response comparison | Consistent; v2 receipt association remains unchanged. |
| 7 | Receiver/emitter integration tests | Association discriminant, exact read, queue and v3 builder | Consistent; exact DB fields precede snapshot construction. |
| 8 | Lifecycle/concurrency tests | Atomic effects, transition dispatch, permission/flag fence and cancellation | Consistent; effect helper runs in the CAE transaction. |
| 9 | Flag/action/access tests | Server facade, all flag fixtures and assignable capability | Consistent; the new flag does not alter ordinary writer selection. |
| 10 | Pure UI and queue tests | Focused editor, fiscal dialog and commercial-list visibility | Consistent; pending rows live in the fiscal queue, not commercial export. |
| 11 | Error/print tests | Exact messages, v3 PDF and read-only audit detail | Consistent; fiscal PDF reads frozen v3 while commercial resolution reads immutable columns/movements. |
| 12 | E2E/full matrix | Type generation, runbook and homologation gate | Consistent; production stays disabled and real ARCA work remains manual. |

## Pre-flight shared-file/interface scan

| Tasks | Producer → consumer | Finding |
|---|---|---|
| 1 → 2 | `PENDIENTE_FISCAL` enum → schema constraints | Ordered correctly; separate transactions. |
| 2 → 4 | period columns/types/guards → pending creation RPC | Ordered correctly; SECURITY DEFINER writer bypasses authenticated guards. |
| 2 → 5 | period row marker → v2/v3 integrity checks | Compatible; pending rows may have null snapshot, reserved period rows require v3. |
| 2 → 8 | permission/effects marker/intent table → transition helper | Compatible; helper owns child writes and hard idempotency markers. |
| 2 → 9 | DB flag/capability RPC → TypeScript facade/user access | Compatible; server continues to recheck DB authority. |
| 2 → 12 | final schema → generated Supabase contracts | Ordered correctly; generation is last. |
| 3 → 4 | line/date/settlement semantics → SQL authoritative mirror | Same modes, exact-cent rule and positive-input/negative-storage convention. |
| 3 → 5 | domain types/mode rules → snapshot-v3 validator | Compatible; v3 includes mode/reason but not commercial settlement. |
| 3 → 7 | A/B/C and `esNotaInterna` → emitter/call sites | Compatible; Task 7 integrates the classifier across persisted rows. |
| 3 → 9 | strict input schema → server action | Compatible; no browser totals, perceptions or issue date. |
| 3 → 10 | domain types/calculation → UI editor/summary | Compatible; UI is advisory and DB remains authoritative. |
| 4 → 7 | pending row/items/intent → exact fiscal read | Compatible; exact read formats PostgreSQL numerics and dates. |
| 4 ↔ 8 | `scripts/test-nota-credito-periodo-fiscal.sh` creation → lifecycle extension | Intentional staged extension; Task 4 owns initial file, Task 8 appends cases. |
| 4 → 9 | creation RPC → server facade | Exact signature is stable before TypeScript wiring. |
| 4 → 10 | pending identity → create-then-open fiscal dialog | Compatible; no receiver or effects are committed by creation. |
| 4 → 12 | schema/RPC → E2E/type generation | Ordered correctly. |
| 5 → 6 | `SnapshotFiscalPersistido` → payload/reconciliation adapter | Compatible; adapter branches on exact version. |
| 5 → 7 | v3 factory/SQL dispatcher → emitter and queue read | Compatible; v2-only validation retained for linked original. |
| 5 → 8 | SQL snapshot dispatcher → transition attempt version | Compatible; `afip_version` remains optimistic revision, not snapshot version. |
| 5 → 11 | validated v3 → printing | Compatible; PDF never trusts live fiscal metadata. |
| 5 → 12 | snapshot migrations/types → generated contracts/full tests | Ordered correctly. |
| 6 → 7 | ARCA payload API → emitter dependency | Compatible; emitter supplies validated persisted union. |
| 6 → 8 | reconciliation decision → `RECUPERAR_CAE` effects | Compatible; recovery and direct approval share one helper. |
| 6 → 11 | request phase/error class → exact human outage copy | Compatible; post-request uncertainty cannot become blind retry. |
| 6 → 12 | adapter fixtures → browser recovery story | Ordered correctly. |
| 7 → 8 | exact association/snapshot read → transition SQL | Compatible; transition validates persisted version rather than reconstructing payload. |
| 7 ↔ 10 | `ventas.index.tsx`, queue projection | Intentional staged edits: Task 7 fixes association classification; Task 10 adds UI visibility/actions. |
| 7 → 11 | queue/exact fields → audit detail | Compatible; sensitive raw errors and intent payload stay excluded. |
| 7 → 12 | emitter/queue behavior → E2E | Ordered correctly. |
| 8 → 10 | fiscal states/actions → queue buttons/messages | Compatible; no action before authorization and no blind retry in reconciliation. |
| 8 → 11 | effects rows/timestamp → audit detail | Compatible; detail derives exact movements and immutable timestamp. |
| 8 → 12 | lifecycle SQL script → E2E/full verification | Ordered correctly. |
| 9 ↔ 10 | `cola.functions.ts`/tests and current-user flags | Intentional staged edits: Task 9 updates contract fixtures; Task 10 adds period UI behavior. |
| 9 → 12 | server action/capability → E2E fixtures/types | Ordered correctly. |
| 10 → 11 | approved list/detail entry → period audit renderer | Compatible; pending is queue-only, approved is commercial-detail visible. |
| 10 → 12 | UI flow → E2E stories | Ordered correctly. |
| 11 → 12 | exact errors/PDF/detail → E2E assertions | Ordered correctly. |

Pre-flight result: no unresolved contradiction against the spec or Global Constraints.

## Task progress

- Task 1: complete (`0ff7638..768fcfc`). Independent review: `Spec: PASS`, `Quality: PASS`; no findings. The implementer used the active local Supabase container name because the hard-coded name from the plan was not present; RED/GREEN queried the correct local database and this environmental adaptation was accepted by review.
- Task 2 review round 1 (`768fcfc..aab246e`): `Spec: FAIL`, `Quality: FAIL`. Important: post-CAE guards only recognized period markers and did not make every row with `OLD.afip_estado = 'APROBADO'` (including its items/payments) immutable to non-owner roles. Important: the SQL contract test used a period row and therefore did not isolate this requirement or prove that transition into `APROBADO` remains allowed. Fix round 1 assigned to the original implementer.
- Task 2 fix round 1: complete in `6eee080`. Added explicit RED/GREEN coverage for an approved non-period row, parent and child DML blocking, and preserved the privileged backfill transition into `APROBADO` through a tightly granted `SECURITY DEFINER` helper.
- Task 2: complete (`768fcfc..6eee080`). Independent re-review: `Spec: PASS`, `Quality: PASS`; no remaining findings. Pre-existing `supabase db lint` finding in `public.cambiar_precios_masivo` remains out of scope and unchanged.
- Task 3 review round 1 (`6eee080..c40ceab`): `Spec: PASS`, `Quality: FAIL`. Important: the pure totals calculator did not independently reject negative/non-finite values or unsupported VAT rates, so direct server use could bypass the Zod schema. Important: monotributo letter selection returned `C` before validating the receiver runtime value. Fix round 1 assigned to the original implementer with regression tests required.
- Task 3 fix round 1: complete in `a8ac3fc`. Added direct runtime validation for line magnitudes, finiteness, VAT allowlist and safe-cent accumulation; receiver runtime validation now precedes monotributo letter selection.
- Task 3: complete (`6eee080..a8ac3fc`). Independent re-review: `Spec: PASS`, `Quality: PASS`; no remaining findings.
- Task 4 review round 1 (`a8ac3fc..7b355e7`): `Spec: FAIL`, `Quality: FAIL`. Important: canonical payload hashing preserved JSON numeric scale, so semantically equal values such as `21` and `21.0` could conflict. Important: `service_role` could directly insert an incomplete `PENDIENTE_FISCAL` header because state was not durably tied to complete period metadata/guard ownership. Important: the required linked-total-reversal regression lived after a pre-existing failure in a legacy suite and therefore never executed. Minor: concurrency temp files lacked cleanup. Fix round 1 assigned to the original implementer.
- Task 4 fix round 1: complete in `cdbfe7e`. Numeric scale is canonicalized, pending-state integrity is tied to period metadata and guarded from direct privileged DML, linked reversal has an independently executable regression suite, and temp-file cleanup was added.
- Task 4 review round 2 (`a8ac3fc..cdbfe7e`): `Spec: PASS`, `Quality: FAIL`. Important: the corrective `SECURITY DEFINER` wrapper normalized unbounded JSON arrays with repeated concatenation before authentication/core size checks, creating an avoidable resource-abuse path. Fix round 2 assigned to the original implementer; require pre-limit behavior and bounded-time oversized-input regression.
- Task 4 fix round 2: complete in `280df14`. Oversized arrays bypass normalization and reach bounded core rejection; in-limit canonicalization is set-based and order-stable, with a timed 10,000-element regression.
- Task 4: complete (`a8ac3fc..280df14`). Independent re-review: `Spec: PASS`, `Quality: PASS`; no remaining findings. Full reset and focused SQL/concurrency/reversal regressions passed.
- Task 5 review round 1 (`280df14..d24b620`): `Spec: FAIL`, `Quality: FAIL`. Important: v3 still persisted `venta.condicionVenta`, violating the explicit boundary that commercial settlement/condition stays outside the fiscal snapshot. Important: reserved/emitting/reconciliation/approved state constraints checked version/identity but neither semantic dispatcher validation nor canonical hash recomputation. Important: TS and PostgreSQL differed on motive whitespace/code-point length. Important: test matrix omitted happy paths for both modes, A/B/C, monotributo projection and invalid rehashed snapshots in mandatory states. Fix round 1 assigned to the original implementer; the explicit commercial-boundary constraint prevails over the brief's inherited-type skeleton.
- Task 5 fix round 1: complete in `d114bf1`. Removed commercial condition from v3 bytes/hash, added semantic+canonical-hash transition trigger, explicit motive code-point rules, full A/B/C/mode/state matrix and canonicalized affected v2 test fixtures.
- Task 5 review round 2 (`280df14..d114bf1`): `Spec: FAIL`, `Quality: FAIL`. Important: the v3 motive still passed through generic `String.trim()` before the explicit fiscal whitespace helper, so TS rejected NBSP-only content that SQL accepted. All other prior findings were closed. Fix round 2 assigned to the original implementer.
- Task 5 fix round 2: complete in `4955022`. V3 motive bypasses generic trimming and uses only the explicit fiscal whitespace/code-point rules; NBSP and EM SPACE parity/hash regressions added.
- Task 5: complete (`280df14..4955022`). Independent re-review: `Spec: PASS`, `Quality: PASS`; no remaining findings. V3 excludes all commercial settlement data, mandatory fiscal states validate canonical semantics/hash, and v2 remains compatible.
- Task 6: complete (`4955022..a1a9d14`). Independent review: `Spec: PASS`, `Quality: PASS`. Two non-blocking Minor follow-ups recorded: replace a partial double-cast v3 reconciliation fixture with a factory-validated fixture, and rename a stale “Writer v2” comment now that the adapter supports persisted v2/v3 snapshots. Payload/reconciliation behavior has no Critical or Important findings.
- Task 7 review round 1 (`a1a9d14..b720ee6`): `Spec: FAIL`, `Quality: FAIL`. Critical in scope: authenticated queue invoked an owner-only persisted-snapshot validator from a `SECURITY INVOKER` function. Important in scope: C projection still sent/compared VAT breakdown instead of `ImpNeto=ImpTotal`, `ImpIVA=0`, no IVA collection. The reviewer also flagged the state-transition RPC as v2-only and demanded a full v3 reserve/approve lifecycle here. Controller scope ruling: the plan explicitly assigns transition snapshot-version dispatch and `RESERVAR/APROBAR/RECUPERAR_CAE` changes to Task 8 (plan lines 986–1079); implementing them in Task 7 would duplicate/violate task ordering. Task 7 must add authenticated queue coverage and successful A/B/C pre-transition/request-projection coverage, while the real transition lifecycle remains an acceptance gate for Task 8. Fix round 1 assigned to the original implementer for the in-scope findings.
- Task 7 fix round 1: complete in `28a947d`. Added audited queue validation boundary with authenticated v2/v3 ACL tests, unified C projection for request/reconciliation, A/B/C pre-transition coverage, and closed Task 6 minor fixture/comment follow-ups.
- Task 7 review round 2 (`a1a9d14..28a947d`): `Spec: FAIL`, `Quality: FAIL`. Important: automatic period-NC receiver selection could fall back to an unconfirmed declared `RESPONSABLE_INSCRIPTO`/`MONOTRIBUTO` condition when padrón returned null, allowing unverified letter/condition selection. Minor: A/B/C tests mocked both preparation and snapshot creation, missing real receiver→letter→v3 composition. Fix round 2 assigned to the original implementer.
- Task 7 fix round 2: complete in `a689f00`. Unconfirmed automatic fallback is limited to CF/Exempt, RI/Monotributo fail closed, and real server composition covers receiver→letter A/B/C→snapshot v3→payload before the Task 8 boundary.
- Task 7: complete (`a1a9d14..a689f00`). Independent re-review: `Spec: PASS`, `Quality: PASS`; no remaining findings. The controller scope ruling was accepted: transition/lifecycle SQL v3 and atomic effects remain exclusively Task 8.
- Task 8 review round 1 (`a689f00..a1ef351`): `Spec: FAIL`, `Quality: FAIL`. Critical: refund-plan rows remained mutable by `service_role`, and the effect helper aggregated then re-read them without locks/hash revalidation, enabling a TOCTOU refund different from the signed intent. Important: `APROBAR` did not require persisted ARCA approval evidence and accepted any non-empty CAE, so a rejected response plus malformed CAE could approve/apply effects. Minor: negative tests did not cover these attacks. Fix round 1 assigned to the original implementer with owner-only plan guards, locked/materialized canonical vector+hash verification, shared approval-evidence validation and no-effects attack regressions.
- Task 8 fix round 1: complete in `c70efbb`. Refund intent is owner-only, locked and materialized once with exact hash verification; APROBAR/RECUPERAR share authoritative approval-evidence/CAE validation; attack, rollback and mutation races added.
- Task 8: complete (`a689f00..c70efbb`). Independent re-review: `Spec: PASS`, `Quality: PASS`; no remaining findings. Atomic CAE/effects, replay, recovery and concurrency guarantees passed review.
- Task 9 review round 1 (`c70efbb..a494eb5`): `Spec: FAIL`, `Quality: FAIL`. Critical: DB effective-capability logic granted any active profile with both booleans even without an actual `user_roles.role='empleado'`, while browser access failed closed; admin assignment could target non-employees. Important: SQL fixtures mislabeled role-less profiles as employees, hiding the discrepancy. Important: the administrative facade returned raw Supabase/SQL `error.message` to UI. Fix round 1 assigned to the original implementer with a forward-only role-aware migration, real-role fixtures/removal cases, recipient-role validation and safe error mapping.
- Task 9 fix round 1: complete in `e0c11e8`. Effective DB capacity now requires a durable employee role (or contractual admin), assignment rejects non-employees, fixtures cover role absence/removal, and administrative errors cross the UI boundary only as safe codes/messages.
- Task 9: complete (`c70efbb..e0c11e8`). Independent re-review: `Spec: PASS`, `Quality: PASS`; no remaining findings.
- Task 10 review round 1 (`e0c11e8..f109951`): `Spec: FAIL`, `Quality: FAIL`. Critical: period NC kept the review button permanently disabled because automatic letter is intentionally null. Important: alternate receiver control was dead in automatic mode; create-then-open and approved→detail were not wired; queue action leaked without period flag/capability; adjustment omitted VAT 2.5/5/27; commercial DB query filtered pending only after limit; tests were mostly helpers and missed mounted flow/double-submit/gating. Minor: field errors lacked inline ARIA/focus and the association summary omitted final amount/receiver/letter. Fix round 1 assigned to the original implementer with mounted behavioral coverage and React/accessibility self-review.
- Task 10 fix round 1: complete in `e0a06e0`. Automatic-letter flow, alternate receiver, create→open, approved→detail, queue gating, six VAT rates, pre-limit query filtering and mounted core tests were added.
- Task 10 review round 2 (`e0c11e8..e0a06e0`): `Spec: FAIL`, `Quality: FAIL`. Important: confirmation summary still omitted net/IVA and planned refund breakdown. Important: controls remained editable while creation was in flight and retries reused the same idempotency key with potentially changed payload. Important: integrated coverage still did not prove stable key+payload retry or all route gates. Minor: editor validation remained global without per-field ARIA/focus. Fix round 2 assigned to the original implementer.
- Task 10 fix round 2: complete in `81e138b`. Added authoritative financial/liquidation preview, frozen key+payload with explicit retry/discard, mounted retry coverage and field-level accessibility scaffolding.
- Task 10 review round 3 (`e0c11e8..81e138b`): `Spec: FAIL`, `Quality: FAIL`. Important: authoritative detail loaded in a parallel optional query and did not gate preview/emission on loading/failure. Important: only editor-local controls froze; route-level branch/client/document/writer controls stayed mutable during an active/ambiguous attempt. Important: Zod failures were still assigned to the wrong field and could expose raw messages. Important: mounted route coverage for flag/capability and linked-vs-period writer exclusivity remained absent. Minor: refund enum labels were raw and React list keys could collide. Fix round 3 assigned to the original implementer.
- Task 10 fix round 3: complete in `cd29821`. Authoritative detail now gates preview/emission with retry, attempt state freezes route-level context/writers, structured field errors/ARIA were added, and liquidation labels/keys were stabilized.
- Task 10 review round 4 (`e0c11e8..cd29821`): `Spec: FAIL`, `Quality: FAIL`. Important: liquidation validation still ran before structural `safeParse`, so combined invalid input focused a downstream refund error instead of the first structural field. Important: the real `ventas.nueva` route still lacked mounted coverage for gates, linked-vs-period writer exclusivity and global attempt locking. Minor: the dialog prop remained fail-open if authoritative detail was omitted for a period row. Per SDD policy, fix round 4 is assigned to a fresh, more capable implementer.
- Task 10 fix round 4: complete in `58cbb3b` by a fresh higher-capability implementer. Structural validation precedes liquidation, nine mounted production-route tests cover gates/writers/global locking, and period dialog detail fails closed when absent.
- Task 10: complete (`e0c11e8..58cbb3b`). Independent re-review: `Spec: PASS`, `Quality: PASS`; no remaining findings. Frontend-design direction and React accessibility/performance checks were applied while preserving the established CasaForma UI.
- Task 11 review round 1 (`58cbb3b..e4efc09`): `Spec: FAIL`, `Quality: FAIL`. Important: `ArcaRespuestaIncierta` was mislabeled as a confirmed ARCA outage; audit rendering could claim there were no cash refunds after the payments read failed; the detail did not distinguish direct CAE authorization from `RECUPERAR_CAE`; and the immutable audit block mixed frozen evidence with mutable operator/client/product labels while accepting an invalid v3 snapshot through a lax parser. PDF v3, v2 compatibility and temporary cleanup passed. Fix round 1 assigned to the original implementer with fail-closed regressions and a closed safe evidence projection.
- Task 11 fix round 1: complete in `bc7902a`. Closed all four prior findings with transport-only outage classification, payment fail-closed gating, a user-bound scalar authorization/recovery projection, canonical snapshot/hash validation and explicit current-label/immutable-ID presentation.
- Task 11 review round 2 (`e4efc09..bc7902a`): `Spec: FAIL`, `Quality: FAIL`. Important: the canonical loader required snapshot v3 even for a freshly created period NC or a safe pre-reservation cancellation, where the lifecycle legitimately has no fiscal identity/snapshot yet, so the required pre-CAE persisted intent became unreadable. All round-1 findings were confirmed closed. Fix round 2 assigned to the original implementer to introduce a strict discriminated unfrozen-intent state while retaining fail-closed behavior for partial/advanced/adulterated fiscal identity.
- Task 11 fix round 2: complete in `e5effb3`. Added exact `INTENCION_NO_CONGELADA` predicates for fresh pending and safe pre-reservation cancellation rows, with complete fiscal-identity absence and an explicit UI discriminant; partial/advanced/no-snapshot rows remain fail-closed.
- Task 11 review round 3 (`bc7902a..e5effb3`): `Spec: FAIL`, `Quality: FAIL`. Important: the frozen branch validated only snapshot hash and sale ID, not parity with persisted fiscal identity or lifecycle completeness; a valid snapshot alongside partial identity, a point association, premature effects or an approved row without CAE/evidence could render contradictory audit data. Fix round 3 assigned to the original implementer to validate the complete frozen lifecycle matrix and derive evidence requirements internally.
- Task 11 fix round 3: complete in `20175d5`. Added shared snapshot/column parity, a closed frozen lifecycle matrix, internal evidence requirements and 35 attack regressions; all prior findings remained closed.
- Task 11 review round 4 (`e5effb3..20175d5`): `Spec: FAIL`, `Quality: FAIL`. Important: recovered-CAE audit used `emision_fiscal_intentos.updated_at` from a `now()` trigger as `confirmadoAt`, while `afip_emitido_at` is subsequently written with `clock_timestamp()` in the same transaction; the valid persisted ordering can therefore be `updated_at < afip_emitido_at`, causing false fail-closed rejection. No other findings. Per SDD round-4 policy, remediation is assigned to a fresh higher-capability implementer with a real SQL recovery/audit regression.
- Task 11 fix round 4: complete in `1b3bc2c` by a fresh higher-capability implementer. Removed `updated_at` as a recovery clock, reads `ventas.afip_emitido_at` user-bound after authorization, preserves the closed attempt proof, and adds a real SQL `RECUPERAR_CAE` regression demonstrating the `now()`/`clock_timestamp()` ordering.
- Task 11: complete (`58cbb3b..1b3bc2c`). Independent re-review: `Spec: PASS`, `Quality: PASS`; no Critical, Important or Minor findings. Final evidence: 372 focused tests, 1,696 full-suite tests, typecheck/lint/format/diff checks and real SQL recovery audit all passed; PDF v2/v3 remained intact.
- Task 12 review round 1 (`1b3bc2c..cf73132`): `Spec: FAIL`, `Quality: FAIL`. Important: the authorized detail server function serialized `afip_snapshot` and `afip_snapshot_hash` to the browser instead of returning a closed presentation DTO. Important: the E2E report overstated coverage because it did not exercise inverted dates, did not persist an ordinary sale with the period flag disabled, and asserted only truncated PDF fragments instead of the frozen CAE/reason/mode/period evidence. Important: global lint remained red and regenerated Supabase types added 239 mechanical findings relative to the base, so the no-new-debt claim was not reproducible. Fix round 1 assigned to the original Task 12 implementer with reserved-key contract coverage, real browser/DB/PDF assertions, and a reproducible generated-artifact/baseline lint gate.
- Task 12 fix round 1: complete. Detail delivery is now an auth-first closed presentation DTO with recursively tested reserved-key exclusion; browser coverage persists the flag-off ordinary sale, isolates inverted dates with zero POST and parses full frozen PDF evidence. Generated Supabase types are formally excluded from lint and a versioned 986/986 no-new-debt gate records the remaining historical debt without claiming global lint is green.
- Task 12 review round 2 (`cf73132..70bbc80`): `Spec: FAIL`, `Quality: FAIL`. Important: the new detail route reused fiscal `PREVISUALIZAR` authorization, so an employee with legitimate Ventas access but without `puede_facturar` could list sales but no longer open any detail. Important: the lint baseline grouped only by file/rule/severity/message and could miss a removed historical violation replaced by an identical new one in the same file; `--write-baseline` was also unanchored. Minor: the reserved-key attack test omitted `secret`, `claim` and `service_role`. Minor: concurrent detail clicks could let a stale slower response replace the latest selection. Fix round 2 assigned to the original Task 12 implementer with RLS-equivalent user-bound read authorization, a base-anchored/diff-aware lint gate and adversarial regression, complete reserved-key coverage, and stale-request protection.
- Task 12 fix round 2: complete. Detail reads now authorize the RLS-visible sale before role/profile/admin access and require active same-branch Ventas visibility without granting fiscal capability; browser coverage proves visible/foreign/fiscal boundaries. The writable grouped lint baseline was replaced by a SHA-anchored, diff-aware identity gate with adversarial compensation tests. Reserved-key attacks now include secret/claim/service_role throughout nested arrays, and `/ventas` ignores stale detail responses with a tested monotonic request token. A full `venta-fiscal.spec.ts` audit also aligned stale selectors/expectations with query-driven dialog opening and authoritative padrón data; the complete OK run is 11 passed, 3 scenario-skipped, 0 failed.
- Task 12 review round 3 (`70bbc80..ca18fd6`): `Spec: FAIL`, `Quality: FAIL`. Important: the no-new-debt lint gate accepted a dirty checkout and used the live ESLint config to lint both current and base bytes, so changing the local anchor to HEAD or disabling a rule could hide debt despite the clean 986/988 result. Minor: the monotonic detail token was not invalidated on route unmount, allowing a late success/error/finally to update state or toast after navigation. Fix round 3 assigned to the original Task 12 implementer with fail-closed clean-checkout/config-isolated lint execution plus tamper/rename/untracked tests, and explicit unmount invalidation covering stale success/error/loading.
- Task 12 fix round 3: complete. The lint entrypoint now refuses any dirty checkout, hardcodes and optionally validates the protected CI anchor, and lints HEAD/base in isolated Node processes with each commit's own ESLint config through a cleaned temporary archive. It also unions the base policy over current lines so disabling a rule in HEAD cannot hide new debt, while retaining the explicit generated-types exclusion; twelve adversarial tests cover staged/unstaged/untracked/config/anchor/rename/replacement/rule-disable/tsconfig isolation attacks. `/ventas` invalidates its monotonic detail token on unmount, suppressing late success, toast and loading updates in four deterministic regressions.
- Task 12: complete (`1b3bc2c..2fbfa3a`). Independent re-review after technical scope clarification: `Spec: PASS`, `Quality: PASS`; no Critical, Important or Minor findings. The reviewer confirmed that in-repository code cannot make itself immutable against a malicious PR or compromised runner; branch protection/external CI remains a rollout recommendation rather than a fiscal-spec blocker.
- Final whole-branch review (`0ff7638..1fea73d`): `Spec: FAIL`, `Quality: FAIL`; no Critical findings. Important: authenticated `cola_fiscal_lectura` serialized backend-only `nc_periodo_payload_hash`, which the browser schema/tests explicitly accepted without a production consumer. Important: historical column grants still let authenticated PostgREST clients select `afip_claim_token`, `afip_claimed_at`, raw fiscal snapshot/hash and sale/anulation idempotency keys/hashes, bypassing the new section-aware server DTO; current `/ventas`, `/reportes`, original-document selection and several server paths still depended on those grants. Final-review fix wave must remove the queue key, migrate legitimate consumers behind auth-first server projections, revoke the eight columns forward-only and prove recursive/real ACL denial before merge.
- Final-review fix wave: implemented, locally verified and awaiting independent re-review. One forward-only migration closes the queue projection/grants and revokes the eight direct column reads; `/ventas`, `/reportes`, original selection, linked NC, PDF and legacy paths now use closed auth-first server projections or exact server-only reads. RED covered the leaked queue hash/reserved keys, browser snapshot dependency and BOLA ordering; GREEN includes reset-from-zero, real authenticated `42501` checks, recursive queue ACL, v2/v3 queue integration, fiscal schema/outbox, period-NC lifecycle, 1,738 unit/route tests, typecheck and production build. This line records remediation evidence only and does not claim re-review.
- Final whole-branch re-review (`1fea73d..2b3ccd3`): `Spec: PASS`, `Quality: PASS`; no Critical, Important or Minor findings. The reviewer confirmed both final findings closed: authenticated queue/browser JSON excludes backend-only evidence while the exact reader remains service-role-only, and all eight sensitive `ventas` columns are denied directly while legitimate consumers use auth-first closed projections. Ready to merge.
